{
  "name": "mongoose-management",
  "version": "0.1.0",
  "description": "Mongoose schemas management tool",
  "bin": "build/index.js",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "npm run build:clean && npm run build:generate && npm run build:template",
    "build:clean": "rm -rf ./build",
    "build:generate": "tsc --project ./tsconfig.build.json",
    "build:template": "copyfiles -u 1 ./src/lib/template/templates/**/*.mst ./src/lib/template/templates/**/*.ts ./build",
    "test": "jest --config=jest.json --coverage 'src'",
    "test:watch": "jest --config=jest.json --watch 'src'",
    "test:ci": "jest --config=jest.json --ci --coverage --verbose 'src'",
    "test:integration": "jest --config=jest.test.js --coverage 'tests'",
    "test:integration:watch": "jest --config=jest.test.js --watch 'tests'",
    "test:integration:ci": "jest --config=jest.test.js --ci --verbose 'tests'",
    "test:all": "npm run test && npm run test:integration",
    "coverage": "npm run coverage:clean && npm run coverage:generate:unit",
    "coverage:integration": "npm run coverage:clean && npm run coverage:generate:integration",
    "coverage:clean": "rm -rf ./coverage",
    "coverage:generate:unit": "jest --config=jest.coverage.js --coverage 'src'",
    "coverage:generate:integration": "jest --config=jest.coverage.js --coverage 'tests'",
    "lint": "npm run prettier && npm run lint:check",
    "lint:check": "tslint --project ./tsconfig.json --config ./tslint.json --format codeFrame",
    "lint:ci": "tslint --project ./tsconfig.json --config ./tslint.json --format codeFrame",
    "prettier": "prettier --config './.prettierrc' --write '.huskyrc' '.prettierrc' './**/*.{ts,tsx,js,jsx,json,md,scss,yml,html}'",
    "prettier:ci": "prettier --config './.prettierrc' --list-different '.huskyrc' '.prettierrc' './**/*.{ts,tsx,js,jsx,json,md,scss,yml,html}'",
    "deploy:major": "npm run deploy:pre && npm version major -m 'Release of version v%s' && npm run deploy:post",
    "deploy:minor": "npm run deploy:pre && npm version minor -m 'Release of version v%s' && npm run deploy:post",
    "deploy:patch": "npm run deploy:pre && npm version patch -m 'Release of version v%s' && npm run deploy:post",
    "deploy:pre": "npm run lint && npm run test:all && npm run build",
    "deploy:post": "git push && git push --tags && npm publish"
  },
  "keywords": [
    "mongodb",
    "mongoose",
    "cli",
    "generator",
    "document",
    "model",
    "schema",
    "database",
    "odm",
    "data",
    "template",
    "management",
    "datastore",
    "query",
    "nosql",
    "orm",
    "db"
  ],
  "author": "Kjell Dießel <development@kettil.de>",
  "contributors": [
    "Kjell Dießel <development@kettil.de>"
  ],
  "license": "MIT",
  "engines": {
    "node": ">= 10"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kettil/mongoose-management"
  },
  "bugs": {
    "url": "https://github.com/kettil/mongoose-management/issues/new"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "copyfiles": "^2.1.1",
    "husky": "^3.0.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.2.0",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.15.0",
    "tslint-immutable": "^6.0.0",
    "tslint-react": "^4.0.0",
    "typescript": "^3.4.4"
  },
  "dependencies": {
    "@types/inquirer": "^6.5.0",
    "@types/mongodb": "^3.1.28",
    "@types/mongoose": "^5.5.6",
    "@types/mustache": "^0.8.32",
    "@types/prettier": "^1.16.4",
    "@types/table": "^4.0.6",
    "@types/yargs": "^13.0.0",
    "chalk": "^2.4.2",
    "inquirer": "^6.5.1",
    "inquirer-fuzzy-path": "^2.1.0",
    "mongodb": "^3.2.7",
    "mongoose": "^5.5.15",
    "mustache": "^3.0.1",
    "ora": "^3.4.0",
    "prettier": "^1.18.2",
    "table": "^5.4.5",
    "yargs": "^13.0.0"
  }
}
